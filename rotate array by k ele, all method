#include <iostream>
using namespace std;

int main() {
	
   int t;
   cin>>t;
   while(t--){
       
       int n;
       cin>>n;
       
       int k;
       cin>>k;
       
       int arr[n];
       for(int i=n-k; i<n; i++){
           cin>>arr[i];
       }
       
        for(int i = 0; i<n-k; i++){
            cin>>arr[i];
        }
        
        for(int i =0; i<n; i++){
            cout<<arr[i]<<" ";
        }
       
       
       
       
              //   LOGIC-----------
           
            //       for(int i=0; i<n; i++){
           //       cout<<arr[(i+k) %n];
          //   }
         //   OR
        //   reverse(nums.begin()+k,nums.end());
       // reverse(nums.begin(),nums.begin()+k);
        
     // OR
            // rotation(nums,D,N);
        
       cout<<endl;
       
   }
	
	return 0;
}
